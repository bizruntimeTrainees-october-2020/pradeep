@model IEnumerable<cricketscore.Models.score>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.pname)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.score1)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.pname)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.score1)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.plno }) |
            @Html.ActionLink("Details", "Details", new { id=item.plno }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.plno })
        </td>
    </tr>
}

</table>
